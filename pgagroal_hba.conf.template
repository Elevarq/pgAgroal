#
# pgagroal host-based authentication
#
# TYPE  DATABASE  USER  ADDRESS      METHOD
#
# The host lines below are generated by the entrypoint from
# PGAGROAL_HBA_SOURCE (comma-separated CIDRs; default = RFC1918 private
# ranges). Set PGAGROAL_HBA_SOURCE=all to accept any source address.
${PGAGROAL_HBA_LINES}
