{
  "Catalog": "AWSMarketplace",
  "ChangeSet": [
    {
      "ChangeType": "AddDeliveryOptions",
      "Entity": {
        "Type": "ContainerProduct@1.0",
        "Identifier": "prod-jl5oxsgdp4rla"
      },
      "DetailsDocument": {
        "Version": {
          "VersionTitle": "1.4.3",
          "ReleaseNotes": "Elevarq packaging v1.4.3, bundling upstream pgagroal 2.1.0. Credential handling hardened: the Helm chart requires an operator-supplied credential source (a Kubernetes Secret is recommended) and ships no default or hardcoded password - it fails to install if no credential source is provided. The hardened shipped defaults from 1.4.0 (scram-sha-256 HBA source allowlist, allow_unknown_users=false, default-on NetworkPolicy) are unchanged. Release notes: https://github.com/Elevarq/pgAgroal/releases/tag/v1.4.3"
        },
        "DeliveryOptions": [
          {
            "DeliveryOptionTitle": "Helm chart (Amazon EKS)",
            "Details": {
              "HelmDeliveryOptionDetails": {
                "CompatibleServices": ["EKS"],
                "ContainerImages": [
                  "709825985650.dkr.ecr.us-east-1.amazonaws.com/elevarq/elevarq-pgagroal:1.4.3"
                ],
                "HelmChartUri": "709825985650.dkr.ecr.us-east-1.amazonaws.com/elevarq/elevarq-pgagroal-chart:1.4.3",
                "Description": "Production-hardened pgagroal connection pooler for PostgreSQL, delivered as a cosign-signed Helm chart. The image ships no default or hardcoded password: you provide your existing PostgreSQL credentials via a Kubernetes Secret, and the chart fails to install if no credential source is given. Hardened shipped defaults: a scram-sha-256 HBA source-address allowlist (no catch-all), allow_unknown_users disabled, and a default-on NetworkPolicy. Runs non-root (UID 1000) with all Linux capabilities dropped, a read-only root filesystem, and seccomp RuntimeDefault. Includes liveness/readiness probes, a PodDisruptionBudget, and an optional Prometheus metrics port. Bundles upstream pgagroal 2.1.0.",
                "UsageInstructions": "1. Authenticate Helm to the AWS Marketplace registry:\n   aws ecr get-login-password --region us-east-1 | helm registry login --username AWS --password-stdin 709825985650.dkr.ecr.us-east-1.amazonaws.com\n\n2. Create a Kubernetes Secret with your EXISTING PostgreSQL credentials. The image ships no default or hardcoded password; these are your own database credentials.\n   kubectl create namespace pgagroal\n   kubectl -n pgagroal create secret generic pgagroal-pg-credentials --from-literal=PG_USERNAME=<app-user> --from-literal=PG_PASSWORD=<app-password>\n\n3. Create a values file named pgagroal-values.yaml (two-space indentation), pointing at your backend and the Secret from step 2:\n\n   postgresql:\n     host: <postgres-host>\n     port: 5432\n   credentials:\n     existingSecret: pgagroal-pg-credentials\n\n   The chart fails to install if no credential source is provided (no empty-password default). If your EKS pod network is outside the RFC1918 ranges (e.g. a 100.64.0.0/10 secondary CIDR), also set pgagroal.hbaSource to that CIDR.\n\n4. Install into your cluster:\n   helm install pgagroal oci://709825985650.dkr.ecr.us-east-1.amazonaws.com/elevarq/elevarq-pgagroal-chart --version 1.4.3 -n pgagroal -f pgagroal-values.yaml\n\n5. Point applications at the pgagroal Service on port 6432. A NetworkPolicy is enabled by default and admits same-namespace clients; for clients in another namespace, set networkPolicy.ingressNamespaceSelectors. Full configuration: https://elevarq.com/docs/pgagroal-container",
                "ReleaseName": "pgagroal",
                "Namespace": "pgagroal"
              }
            }
          }
        ]
      }
    }
  ]
}
